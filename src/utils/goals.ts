import { Goal } from "@actions/goals";

// const goalMapper=(goal:Goal)=>({
//     ...goal,
//     streak:
// })

// export const transformGoals = (goals: Goal[]) => {
//     for (const goal of goals) {
//         const goalItem = { ...goal };
//         goalItem.totalDays = new Array(goal.totalDays).fill({ checked: false });
//     }
// };
